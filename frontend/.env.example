# Leave unset for local dev (vite.config.ts proxies /api to the backend on :8000).
# Set this only when the frontend is deployed separately from the backend.
VITE_API_BASE_URL=https://api.yourdomain.com/api
